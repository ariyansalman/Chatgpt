"""Minimal focused unit tests — do not require Telegram or network.

Run with: `python -m pytest tests/ -q`  (pytest is optional; these files
also run under unittest via `python -m unittest discover -s tests`).
"""
